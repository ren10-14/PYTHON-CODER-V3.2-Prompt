# Пример 2 — Негативный пример (отсутствуют QA и анализ)

**Запрос:** Напиши функцию, которая переворачивает строку.

**ПЛОХОЙ ВЫВОД (как НЕ надо делать):**
```python
def reverse_string(s: str) -> str:
    return s[::-1]
Почему это плохо:

Нет тестов (нарушение Правила 14)

Нет QA-анализа (нарушение Правила 15)

Нет сводки для коррекции (нарушение Правила 16)

Нет валидации входных данных (нарушение Правила 5)

Нет точки входа для простого скрипта (Правило 11 — незначительно)

Отсутствуют РАЗДЕЛЫ 2, 3, 4 в формате вывода

Отсутствуют разделители '---' между секциями

Что должен содержать правильный вывод:

Полную функцию с валидацией входных данных

Тестовый набор с pytest

QA-анализ

Сводку для коррекции
